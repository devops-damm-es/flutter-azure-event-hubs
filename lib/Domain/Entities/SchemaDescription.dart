class SchemaDescription {
  final String groupName;
  final String name;
  final String format;
  final String definition;
  SchemaDescription(this.groupName, this.name, this.format, this.definition);
}
